- add a faint grid overlay thats always visible

- when dragging a point, allow it to snap to an existing LINE SEGMENT as well as to an existing point.
	- Point proximity should keep priority

- Holding ctrl should disable snapping

- add checkbox to mark if a line feature is a bridge => draw it with outline, use for water, crossings over other paths
- add tundra/greymountain/agriculture area types

- feature names are editable in edit tool
- add feature labels visible on map for areas
- add feature labels visible on map for lines